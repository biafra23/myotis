<!-- Daemon logging config. Forced via -Dlogback.configurationFile=logback-daemon.xml
     in the :app `run` task (only when no -Pargs, i.e. the long-running service),
     so it wins over the logback.xml that trueblocks-kotlin bundles on the classpath.

     Console-only: the daemon logs to STDOUT (follow it in the terminal, or via the
     launcher's own redirect). The Rust engine's own tracing is drained to the
     `rust` logger on a short poll (Main.startRustLogPump), so `-Pengine=rust`
     activity shows up here too — set RUST_LOG for more verbosity. Client commands
     (./gradlew :app:run -Pargs=...) use logback-client.xml, also console-only. -->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.jaeckel.ethp2p" level="DEBUG"/>
    <!-- RPC access log: one concise INFO line per JSON-RPC call the server answers
         (method/id/outcome/latency), incl. parse/batch errors. Isolate it above the
         com.jaeckel.ethp2p=DEBUG wire flood with `grep 'jsonrpc.access' devp2p.log`.
         Bump to DEBUG to also log the full request+response bodies. -->
    <logger name="io.myotis.jsonrpc.access" level="INFO"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="org.apache.tuweni" level="WARN"/>
    <logger name="io.libp2p" level="WARN"/>
    <logger name="io.libp2p.security.noise" level="DEBUG"/>
    <logger name="io.libp2p.transport" level="DEBUG"/>
    <logger name="io.libp2p.core" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
