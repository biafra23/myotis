# Track stable. The Gradle probe (root build.gradle.kts) enforces the minimum rustc
# version; pinning an exact version here would force rustup downloads on machines that
# already have a new-enough stable, which fights the "Rust is optional" build design.
[toolchain]
channel = "stable"
